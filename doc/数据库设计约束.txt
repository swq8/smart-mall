1. 表和字段等全部小写，下划线命名方式命名.
2. 表名要有前缀, 表名使用英文单数.
3. 字段尽量不为空(not null); 数据格式时间日期类可为空，字符和数字类均不得为空（not null）.
4. 字段名称一般采用名词或动宾短语
5. 字段命名使用完整名称，禁止缩写(id除外)
6. 在命名表的列时，不要重复表的名称
   例如，在名employe的表中避免使用名为employee_gender的字段
7. 不要在列的名称中包含数据类型